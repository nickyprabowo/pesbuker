const baseURL = "https://jsonplaceholder.typicode.com";
const API = {
    user: `${baseURL}/users`,
    post: `${baseURL}/posts`,
    album: `${baseURL}/albums`,
    photo: `${baseURL}/photos`
}

export default API;